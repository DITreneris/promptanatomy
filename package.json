{
  "name": "promptanatomy",
  "private": true,
  "description": "Root package for Vercel serverless API (api/*). Frontend build via vercel.json.",
  "scripts": {
    "audit:frontend": "cd frontend && npm audit",
    "audit:backend": "cd backend && pip-audit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "stripe": "^17.0.0"
  }
}
