# Changelog

## [Unreleased] / 2026-03-08

### Prideta
- **Mažos įtakos saugumo pakeitimai (planas):** CORS whitelist Vercel `api/` – `api/access.js` ir `api/create-checkout-session.js` naudoja leidžiamų origin sąrašą (`FRONTEND_ORIGIN` + localhost); `Access-Control-Allow-Origin` nustatomas tik jei request origin whitelist'e, kitaip antraštė nestatoma. Prod checklist – `docs/deploy-and-webhook.md` § 2 „Prieš release: produkcijos env checklist“ (STRIPE_WEBHOOK_SECRET, ALLOW_WEBHOOK_WITHOUT_SECRET, FRONTEND_ORIGIN, HTTPS/HSTS, webhook stebėjimas). Root `package.json` – scriptai `audit:frontend`, `audit:backend`. README – pastaba apie `npm audit --production` ir paleidimą iš šaknies `npm run audit:frontend` / `audit:backend`.
- **Legal (2.2):** Privacy Policy ir Terms of Service skeleton puslapiai (`/privacy`, `/terms`) – breadcrumb, antraštė, placeholder tekstas, nuoroda atgal; vertimai `legal.*` (EN/LT). Footer – nuorodos „Privatumo politika“ / „Paslaugų teikimo sąlygos“ (Link), Cookies lieka „(Netrukus)“. Grąžinimo sąlygos jau LP (pricing.refundContact). Failai: `PrivacyPage.jsx`, `TermsPage.jsx`, `App.jsx`, `Footer.jsx`, `en.json`, `lt.json`.
- **Hero juodas blokas (LT):** Kodo kortelės tekstas verčiamas pagal locale – `hero.codeLine1`–`codeLine3`, `codeRole`, `codeRoleValue`, `codeContext`, `codeContextValue`, `codeSchema`, `codeSchemaValue`; LT: TAPATYBĖS_SLUOKSNIS, VAIDMUO, „Vyresnysis DI strategas“, LOGIKOS_APRIBOŽIMAI, KONTEKSTAS, „Įmonių SaaS plėtra...“, DUOMENŲ_STRUKTŪRA, „JSON-Strict | Įdėtas“. Taip pat LT: `liveFeed` „Tiesioginis srautas“, `systemInit` „Sistema startuoja“. `Hero.jsx` naudoja `t('hero....')` vietoj užkoduoto teksto.
- **404 ir klaidų/UX pataisymai (planas):** Catch-all maršrutas – nežinomi keliai nukreipia į `/` (`App.jsx`, `Navigate`). i18n klaidų raktai: `errors.validEmailRequired`, `errors.paymentUnavailable`, `errors.networkError` (en.json, lt.json). SuccessPage – el. laiško išlyga `success.emailDisclaimer` (EN/LT) po pagrindinio teksto; TODO.md – punktas „SuccessPage – nepažadėti el. laiško“ pažymėtas atliktu. Backend GET `/api/access` – el. pašto validacija per Pydantic `AccessQuery` (EmailStr); 400 „Valid email required“ išlaikomas.
- **Premium UI/UX (MUST–SHOULD–WANT):** Dokumentas `docs/ux-premium-practices.md` – geriausios SaaS praktikos (spalvos, šešėliai, micro-interactions), kas įdiegta (MUST) ir tolesni žingsniai (SHOULD/WANT). Įtraukta į `docs/INDEX.md`.
- **Tailwind tema (premium niuansai):** `tailwind.config.js` – spalvos `brand-dark`, `brand-accent`, `brand-accent-hover`; šešėliai `shadow-soft`, `shadow-soft-lg`, `shadow-glow-accent`; `duration-400`. Visi komponentai naudoja `brand-*` vietoj hex.
- **Smooth scroll ir reduced motion:** `index.css` – `html { scroll-behavior: smooth }` anchor nuorodoms; `prefers-reduced-motion` išjungia smooth scroll.
- **Mobilus konversijos UX:** Hero – value line „Master AI prompting for real work“ / „Išmok naudoti AI 10x efektyviau“; viršutinis CTA „See plans“ / „Žiūrėti planus“ (scroll į planus), pirkimo CTA tik kortelėse; vertimai `pricing.planBullets` – 3 value bullet'ai kiekvienam planui (EN/LT); pricing sekcijos vertical rhythm (mažesni atstumai mobiliai, H2 dydis „Pasirinkite planą“).
- **LT viršutinė eilutė:** `nav.brandTagline` lietuviškai – „DI OPERACINĖ SISTEMA“ (anksčiau „DI Operating System“).
- **Mokėjimų geriausios praktikos:** Dokumentas `docs/payment-best-practices.md` – planų žymėjimas (plan_id vs plan_value), Stripe/Supabase/Vercel konvencijos, env checklist, API žemėlapis, pitfalls, plėtros gairės. Įtraukta į `docs/INDEX.md`.
- **Produkcijos prieiga ir checkout pastaba:** Vercel serverless `api/access.js` – GET /api/access?email= (Supabase user_access, CORS). Frontend config – same-origin fallback (kai VITE_API_URL nenustatytas, naudojamas window.location.origin), kad „Patikrinti prieigą“ veiktų be „Failed to fetch“. TODO.md – skyrius „Checkout produkcijoje“: variantas A (VITE_API_URL į FastAPI) arba B (api/create-checkout-session.js Vercel’e).
- **Webhook metadata.plan:** Vercel stripe-webhook priima ir plan_id ("1"–"4"), ir plan_value (3,6,12,15); mapinimas į plan_value, kad Supabase gautų teisingą highest_plan.
- **Vercel–Supabase integracija:** Backend aplinkoje (Vercel arba lokaliai) sukonfigūruoti `SUPABASE_URL` ir `SUPABASE_SERVICE_ROLE_KEY`; Supabase lentelė `user_access` naudojama GET /api/access ir webhook upsert. Stripe webhook – `STRIPE_WEBHOOK_SECRET` įrašytas į .env; po apmokėjimo (`checkout.session.completed`) backend atnaujina prieigą Supabase.
- **Mobilus UI / UX / vartotojo kelionė (LT–EN) auditas:** Dokumentas `docs/audit-mobile-ux-user-journey.md` – mobilus UI (viewport, touch targets, breakpoints, mobile menu), UX (CTA, klaidos, loading, focus trap siūlymas, kontrastas), vartotojo kelionė ir LT/EN nuoseklumas (LocaleContext, vertimai, success/cancel). Įtraukta į `docs/INDEX.md`.
- **MVP upgrade (Supabase + access):** Backend – `GET /api/access?email=` (highest_plan, allowed_modules, can_upgrade_to); Supabase modulis `backend/db.py` (`get_user_access`, `upsert_user_access`); config – `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `plan_id_to_value`, `PLAN_VALUES`. `create-checkout-session` – prieš checkout tikrinama prieiga pagal email; jei jau turi tą patį/aukštesnį planą → 409; Stripe Session su `metadata.plan` ir `client_reference_id`. Webhook `checkout.session.completed` – iš sesijos imamas email ir `metadata.plan`, upsert į `user_access` (new_highest = max(current, purchased)), saugomas `stripe_customer_id`. Frontend – `getAccess(email)` api.js; HomePage – blokas „Patikrinti prieigą“ (email + mygtukas), state `access`/`customerEmail`; Pricing – rodo tik `can_upgrade_to` planus, „Jau turite“ / „Upgrade į X“, perduoda email į checkout. Vertimai: checkAccess, emailPlaceholder, yourAccess, alreadyHave, upgradeTo, errors.accessNotConfigured, errors.alreadyPurchased. Dokumentacija: `docs/supabase-user-access.sql` (lentelė user_access), README/TODO/.env.example atnaujinti. Testai: TestGetAccess (400, 503, 200), test_already_purchased_returns_409.
- **Deploy (produkcija):** Repozitorija nustumta į GitHub ([DITreneris/promptanatomy](https://github.com/DITreneris/promptanatomy)); frontend deploy į Vercel su `vercel.json` (install/build iš `frontend/`, output `frontend/dist`); domenas promptanatomy.app prijungtas per Porkbun DNS (A + CNAME į Vercel). Aplikacija veikia adresu promptanatomy.app / www.promptanatomy.app.
- **Deploy / Webhook:** Dokumentuota, kad Stripe webhook reikalauja viešo backend URL; be deploy (pvz. Vercel arba kito serverio) webhook eventai neturi kur būti siunčiami, todėl `STRIPE_WEBHOOK_SECRET` (whsec_) realiai nebus naudojamas – žr. TODO.
- **Kainodara (4 lygiai):** Backend – `CreateCheckoutBody.plan_id` ("1"|"2"|"3"|"4"), config `stripe_price_id_plan_1` … `_4`, `create-checkout-session` naudoja atitinkamą Stripe Price; frontend – 4 planų kortos (39 / 99 / 149 / 199 €) `Pricing.jsx`, `createCheckoutSession(planId)` per `api.js`; refund tekstas LP pakeistas į „Grąžinimo sąlygos – susisiekite“. Testai: `test_missing_plan_id_returns_422`, `test_invalid_plan_id_returns_422`, `test_plan_not_configured_returns_400`; `test_checkout_not_configured_returns_503` atnaujintas.
- **Kodo bazės analizė (must-should-want):** Testas `test_checkout_not_configured_returns_503` create-checkout-session endpointui, kai Stripe nekonfigūruotas. Skyrius „Rate limiting už reverse proxy“ `docs/security.md` (X-Forwarded-For / patikimas proxy). Funkcija `handle_checkout_completed(session)` backend `main.py` – webhook checkout.session.completed apdorojimas išskirtas į atskirą funkciją su stub DB/email.
- `docs/decisions/README.md` – ADR katalogas su nuoroda į adr-template.
- `CHANGELOG.md` – šis failas.
- **Frontend UX (auditas pagal planą):** skip link „Praleisti į turinį“ → `#main-content`; mobilus meniu (hamburger + drawer) Navbar; breadcrumbs Success/Cancel puslapiuose; meta description ir Open Graph `index.html`; `prefers-reduced-motion` palaikymas `index.css`.
- **Backend (geriausios praktikos):** konfigūracija per Pydantic Settings (`backend/core/config.py`, SecretStr Stripe raktams); `GET /health` health check; pytest testai (`tests/test_limits.py`, `tests/test_api.py`); webhook be `STRIPE_WEBHOOK_SECRET` grąžina 503 (dev leidžiama per `ALLOW_WEBHOOK_WITHOUT_SECRET=1`).
- **Saugumas (must-should-want planas):** projekto šaknyje `.gitignore` (`.env`, `.venv`, `node_modules`, `__pycache__`, `frontend/dist` ir kt.); `docs/security.md` – apžvalga, kas įdiegta, produkcija/HTTPS, priklausomybių auditą; `.github/dependabot.yml` – savaitiniai npm ir pip atnaujinimai; rate limiting (slowapi) – `create-checkout-session` 30/min, `validate-token-limit` 60/min; security headers middleware (`X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`); README skyrius „Priklausomybių saugumo auditą“ (`pip-audit`, `npm audit`); testai `test_invalid_customer_email_returns_422` ir `test_text_over_max_length_returns_422`.
- **Daugiakalbis režimas (LT/EN):** `frontend/src/i18n/LocaleContext.jsx` – React Context su `locale`, `setLocale`, `t(key)` (nested keys, `{{param}}`), `LocaleProvider`; vertimai `frontend/src/i18n/translations/lt.json` ir `en.json`; kalbos perjungiklis LT | EN Navbar; pasirinkimas išsaugomas į `localStorage`; `document.lang`, `<title>` ir meta description atnaujinami pagal locale; visi puslapiai ir komponentai (Navbar, Hero, Methodology, Ecosystem, Pricing, Footer, HomePage, SuccessPage, CancelPage) naudoja `t()`; checkout klaidos „Invalid response“ / „No checkout URL“ verčiamos caller’yje (HomePage).
- **Numatytoji kalba EN ir automatinis nustatymas:** LP kraunasi anglų kalba; pirmą kartą atėjusiam vartotojui kalba nustatoma pagal naršyklę (`navigator.language` / `navigator.languages`); jei nėra `localStorage` – `detectBrowserLocale()` (lt → LT, kitaip EN). `LocaleContext.jsx` – `DEFAULT_LOCALE = 'en'`, fallback EN; `index.html` – `lang="en"`, anglų meta/title pirmam atvaizdavimui. README ir `docs/process/documentation.md` atnaujinti.
- **Kalbos perjungiklis mobile – pirmas:** Mobiliajame drawer kalbos perjungiklis (LT | EN) perkeltas į pirmą poziciją – atsidarius meniu matomas iškart, tada nuorodos, tada CTA. `docs/audit-mobile-ux-user-journey.md` – skyriai „Kalbos perjungiklio vieta (mobile)“ (best practices 2025–2026) ir „Edu / daugiakalbių produktų kontekstas“.
- **Kalbos auditas (EN/LT):** Dokumentas `docs/audit-language-en-lt.md` – gramatika, stilius, kalbos kultūra, įvertinimas vartotojo akimis; įtraukta į `docs/INDEX.md`.
- **Kalbos gairės (LT/EN):** Dokumentas `docs/language-guidelines-en-lt.md` – prekės ženklas (Prompt Anatomy / Promptų Anatomija), AI/DI taisyklė (LT→DI, EN→AI), terminai, tonas; nuoroda į `docs/INDEX.md` ir `docs/process/documentation.md`.
- **Vercel create-checkout-session:** Serverless `api/create-checkout-session.js` – POST /api/create-checkout-session (body: plan_id, customer_email); Supabase prieigos tikrinimas (409 jei jau turi planą); Stripe Checkout Session, CORS; env: STRIPE_SECRET_KEY, STRIPE_PRICE_ID_PLAN_1..4, FRONTEND_ORIGIN, opcionaliai SUPABASE_*.

### Pakeista
- **Saugumo dokumentacija:** `docs/security.md` – CORS aprašymas atnaujintas (Vercel api/ whitelist); nuoroda į prod checklist [deploy-and-webhook.md § 2](docs/deploy-and-webhook.md). `docs/security-audit-deep.md` – S1 (CORS Vercel) pažymėtas įgyvendintu; 4.1 ir jautrūs taškai atnaujinti.
- **api/create-checkout-session.js:** Redirect URL naudoja tik `process.env.FRONTEND_ORIGIN` (be request origin fallback).
- **HomePage ir api.js (klaidų mapinimas, atsakymo apsauga):** Prieigos forma – tuščiam/netinkamam el. paštui rodoma `t('errors.validEmailRequired')`; catch blokuose backend žinutės mapinamos į i18n: „Valid email required“, „Payment provider error“, „Access check not configured“, tinklo klaidos (fetch/network) → `networkError`. `api.js` `getAccess()` – atsakymas normalizuojamas su default `highest_plan: 0`, `allowed_modules` ir `can_upgrade_to` kaip masyvai, kad UI nebūtų sulaužomas neteisingu atsakymu.
- **Micro UI/UX (ultra premium):** Mygtukai – `active:scale-[0.98]`, `duration-200`, CTA `hover:shadow-glow-accent`, `hover:-translate-y-0.5`. Navbar – logo hover `shadow-glow-accent`, nuorodos/kalbų perjungiklis su `brand-*`, focus ring `ring-brand-accent`. Hero – badge `shadow-soft`, code card `shadow-soft-lg`, CTA glow + active scale. Methodology – kortelės `hover:-translate-y-0.5`, `hover:shadow-soft-lg`, `hover:border-brand-accent/40`, ikonų blokas `group-hover:shadow-glow-accent`. Pricing – kortelės hover lift + `shadow-soft-lg`, CTA glow; trust eilutė nuo `opacity-40 grayscale` į `text-slate-400`. Ecosystem – kortelės `hover:-translate-y-0.5`. Footer – nuorodos `hover:text-brand-accent`. Success/Cancel – brand spalvos, vienodi focus ir active scale.
- **Spalvų konsistencija:** Hex (#0B1320, #CFA73A) pakeisti į `brand-dark` / `brand-accent` visuose komponentuose (Navbar, Hero, Methodology, Ecosystem, Pricing, Footer, HomePage, SuccessPage, CancelPage).
- **README.md** – backend struktūroje pridėtas `POST /api/validate-token-limit`; aplinkos kintamųjų lentelėje – `MAX_TOKENS_PER_REQUEST` (optional); pridėti health, Pydantic Settings, webhook prod reikalavimas, backend testai, `ALLOW_WEBHOOK_WITHOUT_SECRET`; pridėtas skyrius „Priklausomybių saugumo auditą“ ir nuoroda į `docs/security.md`.
- **Backend:** `main.py` ir `limits.py` naudoja `core.config` (get_settings) vietoj `os.environ`/`load_dotenv`; webhook endpoint saugumas (503 kai nėra secret).
- **Backend (saugumas):** `CreateCheckoutBody.customer_email` – Pydantic `EmailStr` ir `max_length=254`; `ValidateTokenLimitBody.text` – `max_length=50_000`; CORS `allow_headers` susiaurintas iki `Content-Type`, `Authorization`; pridėtas `slowapi` (rate limit) ir security headers middleware.
- **docs/INDEX.md** – proceso skiltyje pridėta eilutė: ADR katalogas `docs/decisions/`; „Projektas ir paleidimas“ – nuoroda į CHANGELOG.md; pridėta nuoroda į `docs/security.md`.
- **TODO.md** – skyrius „Priežiūra“: priminimas atnaujinti frontend.mdc globs, jei bus naudojamas TypeScript.
- **Frontend UX:** Navbar – „Repo“ rodomas tik kai `GLOSSARY_URL`; focus-visible ring mygtukams ir nuorodoms; touch targets ≥44px; ARIA (aria-live error, aria-busy loading); Footer mirę linkai pakeisti į „Netrukus“; Ecosystem kortelės be cursor-pointer; Success puslapyje neberodomas session_id; Hero avatarams `alt=""`.
- **Kodo bazės analizė (must-should-want):** SuccessPage – tekstas pakoreguotas (nebežadėjimas el. laiško, pridėta „jei ne – susisiekite su mumis“). README struktūroje aiškiai nurodytas entry point `backend/main.py`. HomePage – 10 s timeout po checkout redirect, kad loading būtų nustatomas į false, jei peradresavimas neįvyksta.
- **SuccessPage (LT/EN):** Vertimai `success.body` – nebežadėjama el. laiško / instrukcijų; EN: „Payment received. Your access is being set up. If you have any questions, please contact us.“; LT: „Apmokėjimas gautas. Jūsų prieiga ruošiama. Jei turite klausimų – susisiekite su mumis.“ (`frontend/src/i18n/translations/en.json`, `lt.json`).
- **docs/security.md:** Dev webhook – paaiškinta, kad su `ALLOW_WEBHOOK_WITHOUT_SECRET=1` payload vis tiek apdorojamas (JSON parse, checkout.session.completed → user_access upsert), bet parašas netikrinamas; naudoti tik lokaliai.
- **Frontend copy auditas (LP):** SuccessPage – copy be el. laiško žadėjimo („Prieiga suteikiama pagal nustatymus“, „per 24 val. negavote instrukcijų – susisiekite“). Pricing – „Visi planai įskiepia:“ → „Visi planai apima:“. Footer – © dinaminiai metai (`getFullYear()`), prekės ženklas „Promptų Anatomija“, nav „Ekosistema“ ir „Kainodara“. Navbar – „Ekosistema“ vietoj „Sistemos“, „Kainodara“ vietoj „Pricing“. Methodology – „Scale“ → „Skalavimas“. HomePage – konkretesnis klaidos pranešimas: „Nepavyko prisijungti prie mokėjimo. Bandykite vėliau.“
- **Dokumentacija (i18n):** README – skyrius „Daugiakalbis režimas (LT/EN)“ (vertimų kelias, kaip pridėti tekstą); `docs/process/documentation.md` – i18n raktų struktūra; `docs/INDEX.md` – nuoroda į frontend i18n; TODO.md – pažymėtas atliktas daugiakalbis režimas.
- **Vertimai (LT) po kalbos audito:** `lt.json` – „Praleisti į turinį“ → „Pereiti prie turinio“; „Nustok kalbėtis.“ → „Nustok kalbėti.“; „500+ narys“ → „500+ narių“; „Sėkmingas Diegimas“ → „Sėkmingas diegimas“; „Upgrade į %s“ → „Pagerinti iki %s“; „Vertikalūs Varikliai“ → „Vertikalūs varikliai“; sertifikatas „nuo 70%“ → „nuo 70% įvertinimo“. **EN** `en.json` – „Certificate (from 70%)“ → „Certificate (from 70% score)“. `docs/audit-language-en-lt.md` – skyrius „Įgyvendintos pataisos (po peržiūros)“.
- **EN pavadinimas ir LT AI→DI:** EN – vartotojui matomas pavadinimas „Prompt Anatomy“ (`en.json`: meta, brand, footer.brand, copyright, pricing.subtext, cancel.body; navbar PROMPT/ANATOMY; `index.html` title/meta/og). LT – visur vartotojui matomoje kalboje „AI“ pakeista į „DI“ (`lt.json`: meta, brandTagline, hero, methodology, ecosystem, aiPowered, footer.tagline). Kalbos gairėse įrašyta AI/DI taisyklė.

### Taisymai
- **Backend ir frontend klaidų rodymas:** Backend klaidos („Valid email required“, „Payment provider error“, tinklo klaidos) dabar rodomos per i18n atitinkamais raktais, o ne raw žinute (LT/EN vartotojui).
- **Mobilus šoninis meniu (užstringimas):** Nuimtas `backdrop-blur-sm` iš mobile overlay – pilno ekrano blur mobiliajame apkrauna GPU ir sukeldavo lagą. Overlay dabar tik `bg-black/50`, meniu atsidaro/užsidaro sklandžiai.
- **Backend webhook dev režimas:** Kai `STRIPE_WEBHOOK_SECRET` nenustatytas ir `ALLOW_WEBHOOK_WITHOUT_SECRET=1`, handleris anksčiau grąžindavo `{"received": True}` be payload apdorojimo – Supabase `user_access` neatsinaujindavo. Dabar payload parsina kaip JSON ir, jei `event.type === "checkout.session.completed"`, kviečia `handle_checkout_completed(event["data"]["object"])`; parašas netikrinamas (log įspėjimas). `backend/main.py`.
- **frontend/src/api.js:** Sėkmės keliu saugus atsakymo parsavimas – `res.json().catch(() => null)` ir tikrinama `data?.url`, kad 200 su ne-JSON ar be `url` nesukeltų neapdorotos rejection.
