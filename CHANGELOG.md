# Changelog

## [Unreleased] / 2025-03-07

### Prideta
- **Deploy / Webhook:** Dokumentuota, kad Stripe webhook reikalauja viešo backend URL; be deploy (pvz. Vercel arba kito serverio) webhook eventai neturi kur būti siunčiami, todėl `STRIPE_WEBHOOK_SECRET` (whsec_) realiai nebus naudojamas – žr. TODO.
- **Kainodara (4 lygiai):** Backend – `CreateCheckoutBody.plan_id` ("1"|"2"|"3"|"4"), config `stripe_price_id_plan_1` … `_4`, `create-checkout-session` naudoja atitinkamą Stripe Price; frontend – 4 planų kortos (39 / 99 / 149 / 199 €) `Pricing.jsx`, `createCheckoutSession(planId)` per `api.js`; refund tekstas LP pakeistas į „Grąžinimo sąlygos – susisiekite“. Testai: `test_missing_plan_id_returns_422`, `test_invalid_plan_id_returns_422`, `test_plan_not_configured_returns_400`; `test_checkout_not_configured_returns_503` atnaujintas.
- **Kodo bazės analizė (must-should-want):** Testas `test_checkout_not_configured_returns_503` create-checkout-session endpointui, kai Stripe nekonfigūruotas. Skyrius „Rate limiting už reverse proxy“ `docs/security.md` (X-Forwarded-For / patikimas proxy). Funkcija `handle_checkout_completed(session)` backend `main.py` – webhook checkout.session.completed apdorojimas išskirtas į atskirą funkciją su stub DB/email.
- `docs/decisions/README.md` – ADR katalogas su nuoroda į adr-template.
- `CHANGELOG.md` – šis failas.
- **Frontend UX (auditas pagal planą):** skip link „Praleisti į turinį“ → `#main-content`; mobilus meniu (hamburger + drawer) Navbar; breadcrumbs Success/Cancel puslapiuose; meta description ir Open Graph `index.html`; `prefers-reduced-motion` palaikymas `index.css`.
- **Backend (geriausios praktikos):** konfigūracija per Pydantic Settings (`backend/core/config.py`, SecretStr Stripe raktams); `GET /health` health check; pytest testai (`tests/test_limits.py`, `tests/test_api.py`); webhook be `STRIPE_WEBHOOK_SECRET` grąžina 503 (dev leidžiama per `ALLOW_WEBHOOK_WITHOUT_SECRET=1`).
- **Saugumas (must-should-want planas):** projekto šaknyje `.gitignore` (`.env`, `.venv`, `node_modules`, `__pycache__`, `frontend/dist` ir kt.); `docs/security.md` – apžvalga, kas įdiegta, produkcija/HTTPS, priklausomybių auditą; `.github/dependabot.yml` – savaitiniai npm ir pip atnaujinimai; rate limiting (slowapi) – `create-checkout-session` 30/min, `validate-token-limit` 60/min; security headers middleware (`X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`); README skyrius „Priklausomybių saugumo auditą“ (`pip-audit`, `npm audit`); testai `test_invalid_customer_email_returns_422` ir `test_text_over_max_length_returns_422`.
- **Daugiakalbis režimas (LT/EN):** `frontend/src/i18n/LocaleContext.jsx` – React Context su `locale`, `setLocale`, `t(key)` (nested keys, `{{param}}`), `LocaleProvider`; vertimai `frontend/src/i18n/translations/lt.json` ir `en.json`; kalbos perjungiklis LT | EN Navbar; pasirinkimas išsaugomas į `localStorage`; `document.lang`, `<title>` ir meta description atnaujinami pagal locale; visi puslapiai ir komponentai (Navbar, Hero, Methodology, Ecosystem, Pricing, Footer, HomePage, SuccessPage, CancelPage) naudoja `t()`; checkout klaidos „Invalid response“ / „No checkout URL“ verčiamos caller’yje (HomePage).

### Pakeista
- **README.md** – backend struktūroje pridėtas `POST /api/validate-token-limit`; aplinkos kintamųjų lentelėje – `MAX_TOKENS_PER_REQUEST` (optional); pridėti health, Pydantic Settings, webhook prod reikalavimas, backend testai, `ALLOW_WEBHOOK_WITHOUT_SECRET`; pridėtas skyrius „Priklausomybių saugumo auditą“ ir nuoroda į `docs/security.md`.
- **Backend:** `main.py` ir `limits.py` naudoja `core.config` (get_settings) vietoj `os.environ`/`load_dotenv`; webhook endpoint saugumas (503 kai nėra secret).
- **Backend (saugumas):** `CreateCheckoutBody.customer_email` – Pydantic `EmailStr` ir `max_length=254`; `ValidateTokenLimitBody.text` – `max_length=50_000`; CORS `allow_headers` susiaurintas iki `Content-Type`, `Authorization`; pridėtas `slowapi` (rate limit) ir security headers middleware.
- **docs/INDEX.md** – proceso skiltyje pridėta eilutė: ADR katalogas `docs/decisions/`; „Projektas ir paleidimas“ – nuoroda į CHANGELOG.md; pridėta nuoroda į `docs/security.md`.
- **TODO.md** – skyrius „Priežiūra“: priminimas atnaujinti frontend.mdc globs, jei bus naudojamas TypeScript.
- **Frontend UX:** Navbar – „Repo“ rodomas tik kai `GLOSSARY_URL`; focus-visible ring mygtukams ir nuorodoms; touch targets ≥44px; ARIA (aria-live error, aria-busy loading); Footer mirę linkai pakeisti į „Netrukus“; Ecosystem kortelės be cursor-pointer; Success puslapyje neberodomas session_id; Hero avatarams `alt=""`.
- **Kodo bazės analizė (must-should-want):** SuccessPage – tekstas pakoreguotas (nebežadėjimas el. laiško, pridėta „jei ne – susisiekite su mumis“). README struktūroje aiškiai nurodytas entry point `backend/main.py`. HomePage – 10 s timeout po checkout redirect, kad loading būtų nustatomas į false, jei peradresavimas neįvyksta.
- **Frontend copy auditas (LP):** SuccessPage – copy be el. laiško žadėjimo („Prieiga suteikiama pagal nustatymus“, „per 24 val. negavote instrukcijų – susisiekite“). Pricing – „Visi planai įskiepia:“ → „Visi planai apima:“. Footer – © dinaminiai metai (`getFullYear()`), prekės ženklas „Promptų Anatomija“, nav „Ekosistema“ ir „Kainodara“. Navbar – „Ekosistema“ vietoj „Sistemos“, „Kainodara“ vietoj „Pricing“. Methodology – „Scale“ → „Skalavimas“. HomePage – konkretesnis klaidos pranešimas: „Nepavyko prisijungti prie mokėjimo. Bandykite vėliau.“
- **Dokumentacija (i18n):** README – skyrius „Daugiakalbis režimas (LT/EN)“ (vertimų kelias, kaip pridėti tekstą); `docs/process/documentation.md` – i18n raktų struktūra; `docs/INDEX.md` – nuoroda į frontend i18n; TODO.md – pažymėtas atliktas daugiakalbis režimas.

### Taisymai
- **frontend/src/api.js:** Sėkmės keliu saugus atsakymo parsavimas – `res.json().catch(() => null)` ir tikrinama `data?.url`, kad 200 su ne-JSON ar be `url` nesukeltų neapdorotos rejection.
